# Token somente de leitura, criado em DatoCMS > Settings > API Tokens.
# Habilite a permissão "Access the Content Delivery API".
DATOCMS_API_TOKEN=

# Opcional: use apenas para conteúdo em outro environment do DatoCMS.
DATOCMS_ENVIRONMENT=
